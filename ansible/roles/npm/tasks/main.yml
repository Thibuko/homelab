---
- name: Create npm directory
  ansible.builtin.file:
    path: /docker/npm
    state: directory
    mode: '0755'

- name: Template docker compose file
  ansible.builtin.template:
    src: compose.yml.j2
    dest: /docker/npm/compose.yml
    mode: '0644'

- name: Manage npm stack
  community.docker.docker_compose_v2:
    project_src: /docker/npm
    state: "{{ 'absent' if service_state == 'absent' else 'present' }}"
    restarted: "{{ service_state == 'restarted' }}"
    pulled: "{{ service_state == 'pulled' }}"
