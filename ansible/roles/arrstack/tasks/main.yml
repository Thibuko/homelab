---
- name: Create arrstack directory
  ansible.builtin.file:
    path: /docker/arrstack
    state: directory
    mode: '0755'

- name: Template docker compose file
  ansible.builtin.template:
    src: compose.yml.j2
    dest: /docker/arrstack/compose.yml
    mode: '0644'

- name: Manage arrstack stack
  community.docker.docker_compose_v2:
    project_src: /docker/arrstack
    state: "{{ 'absent' if service_state == 'absent' else 'present' }}"
    restarted: "{{ service_state == 'restarted' }}"
    pulled: "{{ service_state == 'pulled' }}"
